const { Client } = require('discord.js'); //install things you might required here

module.exports = {
    name: "name", //it must be name of an event look at event guide on github
    rest: false,
    once: false,
    /**
     * @param { Client } client
     * @param { GuildMember } member
     */
    async execute(member, client) {
        //your code / logic here
      
    }
}